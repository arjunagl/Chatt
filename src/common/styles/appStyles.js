import styled from 'styled-components';

const BlockStyle = styled.div`
  display: flex;
  flex-direction: column;
  align-items: center;
`;

const ElementStyle = styled.div`
  border: solid;
`;

const ItemStyle = styled.div`
  border: solid;
`;

export { BlockStyle, ElementStyle, ItemStyle };
